name: Continupus Integration and Delpoy

on:
  push:
    branches:
      -main

jobs:
  build: 
  
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
      - uses: actions/checkout@v2
      

      - name: Setup .Net 5
      - uses: actions/setup-dotnet@v1
      with:
      dotnet-version: '5.0.x'

      -name: Restore dependencies
      ru: dotnet restore

      # -name: Build app
      #  run: dotnet build-c Release --no-restore

       

